import { View, Text } from "react-native";

const MoreInfos = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ fontSize: 20, color: "red" }}>
        and this project is so difficult :O
      </Text>
    </View>
  );
};

export default MoreInfos;
